---
import AdminLayout from "../../../layouts/AdminLayout.astro";
import AdminShell from "../../../components/admin/AdminShell.astro";
import FaqsModuleEditor from "../../../components/admin/editors/FaqsModuleEditor";
import { MODULE_SECTIONS } from "../../../components/admin/sections";

const section = MODULE_SECTIONS.find((item) => item.key === "mod-faqs")!;
---

<AdminLayout title={`${section.label} module`}>
  <AdminShell
    active={section.key}
    title={`${section.label} module`}
    lede={section.blurb}
    publicHref={section.publicHref}
  >
    <FaqsModuleEditor client:load />
  </AdminShell>
</AdminLayout>
