---
export const prerender = false;
import AdminLayout from "../../../layouts/AdminLayout.astro";
import AdminShell from "../../../components/admin/AdminShell.astro";
import PackagesModuleEditor from "../../../components/admin/editors/PackagesModuleEditor";
import { MODULE_SECTIONS } from "../../../components/admin/sections";

const section = MODULE_SECTIONS.find((item) => item.key === "mod-packages")!;
---

<AdminLayout title={`${section.label} module`}>
  <AdminShell
    active={section.key}
    title={section.label}
    heading={`${section.label} module`}
    lede={section.blurb}
    publicHref={section.publicHref}
  >
    <PackagesModuleEditor client:load />
  </AdminShell>
</AdminLayout>
