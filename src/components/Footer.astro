---
import type { NavItem } from "./islands/MobileNav";
import Button from "./Button.astro";
import { ENQUIRY_HREF } from "../lib/navigation";

interface Props {
  items: NavItem[];
  brandName: string;
  serviceAreaStatement: string;
  reviewUrl: string | null;
  footerCta: { title: string; lede: string; label: string };
}
const { items, brandName, serviceAreaStatement, reviewUrl, footerCta } = Astro.props;
const year = new Date().getFullYear();
---

<footer class="site-footer on-dark">
  <div class="footer-glow" aria-hidden="true"></div>
  <div class="container footer-top">
    <div class="footer-brand">
      <p class="brand-name">
        <span class="brand-mark" aria-hidden="true">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.6"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <rect x="2.5" y="5.5" width="19" height="13" rx="3.5" />
            <circle cx="12" cy="12" r="3.4" />
            <path d="M7.5 5.5 9 3.2h6l1.5 2.3" />
          </svg>
        </span>
        {brandName}
      </p>
      <p class="brand-statement">{serviceAreaStatement}</p>
      {reviewUrl ? (
        <p class="footer-review">
          <a href={reviewUrl}>Leave a Google review</a>
        </p>
      ) : null}
    </div>
    <nav class="footer-nav" aria-label="Explore">
      <h2>Explore</h2>
      <ul>
        {items.map((item) => (
          <li>
            <a href={item.href}>{item.label}</a>
          </li>
        ))}
      </ul>
    </nav>
    <nav class="footer-nav" aria-label="Legal">
      <h2>Legal</h2>
      <ul>
        <li>
          <a href="/privacy">Privacy Policy</a>
        </li>
        <li>
          <a href="/terms">Terms &amp; Conditions</a>
        </li>
      </ul>
    </nav>
    <div class="footer-cta">
      <h2>{footerCta.title}</h2>
      <p>{footerCta.lede}</p>
      <Button href={ENQUIRY_HREF} variant="primary" tone="dark" size="lg" arrow>
        {footerCta.label}
      </Button>
    </div>
  </div>
  <div class="container footer-base">
    <p class="footer-copy">
      &copy; {year} {brandName}. All rights reserved.
    </p>
    <p class="footer-area">{serviceAreaStatement}</p>
  </div>
</footer>
<style>
  .site-footer {
    position: relative;
    overflow: hidden;
    background: var(--color-ink-deep);
    color: var(--color-on-ink);
    padding-block: clamp(3rem, 6vw, 5rem) var(--space-md);
    border-top: 1px solid var(--color-ink-hairline);
  }
  .footer-glow {
    position: absolute;
    inset: auto 0 0 0;
    height: 60%;
    background: radial-gradient(90% 120% at 15% 120%, rgba(230, 206, 138, 0.1), transparent 70%);
    pointer-events: none;
  }
  .footer-top {
    position: relative;
    display: grid;
    gap: var(--space-lg);
    grid-template-columns: 1fr;
  }
  .brand-name {
    display: flex;
    align-items: center;
    gap: var(--space-2xs);
    margin: 0 0 var(--space-xs);
    font-family: var(--font-display);
    font-size: 1.45rem;
    font-weight: 500;
    letter-spacing: var(--tracking-tight);
    color: var(--color-on-ink);
  }
  .brand-mark {
    display: inline-flex;
    color: var(--color-accent-on-dark);
  }
  .brand-statement {
    margin: 0;
    max-width: 30rem;
    color: var(--color-on-ink-secondary);
  }
  .footer-review {
    margin: var(--space-sm) 0 0;
  }
  .footer-review a {
    color: var(--color-accent-on-dark);
    text-underline-offset: 4px;
  }
  .footer-nav h2 {
    margin: 0 0 var(--space-sm);
    color: var(--color-on-ink);
    font-family: var(--font-body);
    font-size: var(--text-caption);
    font-weight: 600;
    letter-spacing: var(--tracking-eyebrow);
    text-transform: uppercase;
  }
  .footer-nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-xs);
  }
  .footer-nav a {
    position: relative;
    display: inline-block;
    color: var(--color-on-ink-secondary);
    text-decoration: none;
    transition: color var(--motion-quick) var(--ease-default);
  }
  .footer-nav a::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -2px;
    height: 1px;
    background: var(--color-accent-on-dark);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--motion-settled) var(--ease-default);
  }
  .footer-nav a:hover {
    color: var(--color-accent-on-dark);
  }
  .footer-nav a:hover::after {
    transform: scaleX(1);
  }
  .footer-cta p {
    margin: 0 0 var(--space-md);
    max-width: 22rem;
    color: var(--color-on-ink-secondary);
  }
  .footer-cta h2 {
    margin: 0 0 var(--space-2xs);
    font-size: var(--text-h3);
    color: var(--color-on-ink);
  }
  .footer-base {
    position: relative;
    margin-top: var(--space-xl);
    padding-top: var(--space-md);
    border-top: 1px solid rgba(244, 238, 224, 0.1);
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2xs) var(--space-md);
    justify-content: space-between;
    align-items: baseline;
  }
  .footer-base p {
    margin: 0;
    color: var(--color-on-ink-faint);
    font-size: var(--text-small);
  }
  @media (min-width: 720px) {
    .footer-top {
      grid-template-columns: 1.6fr 0.8fr 0.8fr;
    }
    .footer-cta {
      grid-column: 1 / -1;
      padding-top: var(--space-lg);
      border-top: 1px solid rgba(244, 238, 224, 0.1);
    }
  }
  @media (min-width: 1080px) {
    .footer-top {
      grid-template-columns: 1.5fr 0.8fr 0.8fr 1.2fr;
      align-items: start;
    }
    .footer-cta {
      grid-column: auto;
      padding-top: 0;
      border-top: 0;
    }
  }
</style>
